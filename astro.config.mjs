// @ts-check
import { defineConfig } from 'astro/config';

// Minimal config to get the dev server running.
// Output target, production `site` URL, and the sitemap integration
// are added in the "Configure Astro" task.
export default defineConfig({});
